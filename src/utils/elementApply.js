import Vue from 'vue'

import 'element-ui/lib/theme-chalk/index.css'

import {
   Container,
   Header,
   Aside,
   Main,
   Switch,
   Menu,
   MenuItem,
   MenuItemGroup,
   Submenu,
   Pagination,
   Tabs,
   TabPane,
   Table,
   TableColumn,
   Slider,
   Input,
   Popover,
   Button,
   Form,
   FormItem,
   Drawer,
   InfiniteScroll,
   Dialog,
   Link,
   Tooltip,
   Loading,
   Footer,
} from 'element-ui';

Vue
   .use(Container)
   .use(Header)
   .use(Aside)
   .use(Main)
   .use(Switch)
   .use(Menu)
   .use(MenuItem)
   .use(MenuItemGroup)
   .use(Submenu)
   .use(Pagination)
   .use(Tabs)
   .use(TabPane)
   .use(Table)
   .use(TableColumn)
   .use(Slider)
   .use(Input)
   .use(Popover)
   .use(Button)
   .use(Form)
   .use(FormItem)
   .use(Drawer)
   .use(InfiniteScroll)
   .use(Dialog)
   .use(Link)
   .use(Tooltip)
   .use(Loading)
   .use(Footer)